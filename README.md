# Node Fetch

Node Fetch is a simple HTTP client for Node.js, inspired by Axios. It allows you to make HTTP requests with ease, including support for request/response interceptors and error handling.

## Installation

You can install the library via npm:

```bash
npm install node-fetch
```
